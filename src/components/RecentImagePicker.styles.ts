import { StyleSheet } from 'react-native';

export default () => {
  return StyleSheet.create({
    image: {
      width: 100,
      height: 100,
      margin: 5,
      borderRadius: 10,
    },
  });
};
